{
  "name": "proxy",
  "version": "1.0.0",
  "description": "Self-host proxy for Otakudesu Mobile",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node -e \"fetch('http://127.0.0.1:8787/health').then(r=>r.json()).then(d=>console.log('Proxy OK:',d)).catch(e=>console.error('Proxy tidak aktif di port 8787'))\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cheerio": "^1.2.0",
    "express": "^5.2.1"
  }
}
